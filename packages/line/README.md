Text-only LINE Messenger adapter for OpenTag.

Supports webhook signature verification, text message normalization, local ingress, and push-message callbacks. Media, Flex messages, rich menus, and `/bind` are intentionally out of scope.
